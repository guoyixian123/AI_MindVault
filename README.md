# ai_major

## 项目介绍

这是一个基于 **LangChain4j** 做的个人法律智能助手，核心思路是用 **RAG（检索增强生成）** 来提升回答的准确性，尽量减少大模型“胡说”的情况。

项目支持流式输出、会话隔离和会话记忆持久化，同时把本地法律资料做向量化存到 Redis 里，用来做语义检索。

整体上不局限于法律场景，通过改 system 角色和知识库，也可以扩展成医生、金融顾问之类的智能体。
![输入图片说明](%E6%88%AA%E5%B1%8F2026-03-27%2022.23.12.png)
![输入图片说明](%E6%88%AA%E5%B1%8F2026-03-27%2022.24.02.png)

---

## 技术栈

* Java
* Spring Boot
* SSM（Spring + SpringMVC + MyBatis）
* LangChain4j
* Redis（向量存储）
整体采用单体架构
![样图1](%E6%88%AA%E5%B1%8F2026-03-27%2023.19.16.png)


---

## 功能大致覆盖

### 劳动相关

* 劳动合同是否合法（试用期、竞业限制这些）
* 工资拖欠、加班费怎么维权
* 被辞退时 N+1 / 2N 怎么算，仲裁有没有过期

### 消费维权

* 七天无理由退货哪些能用、哪些不行
* 预付卡退费（餐饮、医美、培训）
* 直播带货虚假宣传怎么留证据

### 民间借贷

* 借条/欠条怎么写才有效
* 利息怎么算（LPR 四倍）
* 微信转账没有备注怎么补证据

### 婚恋家事

* 彩礼什么情况下可以要回来
* 婚内借款算不算个人债务
* 抚养权一般看哪些证据

---

## 核心设计

### 1. system 角色

可以自己定义 AI 的角色，比如法律顾问、医生、理财顾问等等。
修改路径下的system即可
![输入图片说明](%E6%88%AA%E5%B1%8F2026-03-27%2022.36.17.png)
### 2. RAG 知识库

把数据放在 `content/` 目录里，做向量化后存进 Redis，用来做检索。
这样 AI 回答会更贴近真实资料,可以防止ai幻觉，而不是纯靠模型生成。

---

## 使用说明

### 1. 配置

在 yml 里填好：

* API Key
* 模型地址
* 模型名称

![输入图片说明](%E6%88%AA%E5%B1%8F2026-03-27%2022.59.31.png)
---

### 2. 环境准备

需要安装 Redis Stack（支持向量存储）

---

### 3. 数据处理

把 `content/` 里的数据做向量化存进 Redis

注意：
向量化做完之后可以把相关 `@Bean` 注释掉，不然每次启动都会重新跑，比较浪费 token。

rag数据可以从这里找：
[https://modelscope.cn/datasets](https://modelscope.cn/datasets)

---

### 4. 启动

配置完直接启动，就可以用了。

---

## 简单总结一下

这个项目主要是：

* 把 LangChain4j + RAG 跑通
* 用 Redis 做向量数据库
* 做了会话记忆和隔离
* 场景偏实际（法律问题）

也比较方便改造成其他类型的智能体。
